# nixpacks.toml

[phases.setup]
# Instala Node, Python3, FFmpeg e o yt-dlp nativo do sistema Linux
nixPkgs = ["nodejs", "python3", "ffmpeg", "yt-dlp"]

[phases.install]
cmds = ["npm ci"]

[phases.build]
cmds = ["npm run build"]

[start]
cmd = "npm start"